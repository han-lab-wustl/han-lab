function [pre,post,rew] = get_place_cell_density(eprng, pcs, coms)
first_trials = eprng(trialnum(eprng)>2 & trialnum(eprng)<8);
last_trials = eprng(trialnum(eprng)>max(trialnum(eprng))-5);
dff_first = dFF(first_trials, pcs); % only place cells
dff_last = dFF(last_trials, pcs); % only place cells
ypos_f = ybinned(first_trials);
ypos_l = ybinned(last_trials);
dff_com_f = [];
for pc=1:length(pcs)
dff_com_f(pc) = dff_first(ypos_f<)

end